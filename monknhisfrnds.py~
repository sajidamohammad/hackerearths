test=int(raw_input())

for i in range(test):
	nm=map(int,raw_input().split(" "))
frnds=map(int,raw_input().split(" "))
if nm[0]+nm[1]==len(frnds):
	for j in range(len(frnds)-nm[0]):
		i=nm[0]
		if frnds[nm[0]+j] in frnds[:i]:
			print "YES"
		i=i+1
		else:
			print "NO"
